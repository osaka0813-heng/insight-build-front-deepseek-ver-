# Build014.2 Recovery Frontend

- Requires backend `014.2-recovery-global`.
- Rejects the previously deployed 012.6 backend as incompatible.
- Keeps only Global editorial snapshots.
- Removes the Japanese Writer checkpoint from automation types.
- Preserves the manual editorial console as a recovery and review tool.
- Daily generation and publishing now run independently on the backend.
